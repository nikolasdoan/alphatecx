import { Assistant } from "../assistant";

/* The chat moved here from `/` on 2026-09-16, when `/` became the public
   landing page. The component is unchanged — only the route. */
export default function ChatPage() {
	return <Assistant />;
}
